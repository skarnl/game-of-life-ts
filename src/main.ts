/**
 * The main file, which is the base for this GOL reimplementation in TS
 */

import { Grid } from 'app/Grid';

const grid: object = Grid();

window.console.log(grid);
window.console.log('fdsljksfjlkfsd');
