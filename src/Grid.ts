/**
 * fsdlkjfs
 */

// import { Dimensions } from 'src/Dimensions';

// export const Grid = (dimensions: Dimensions): void => {
//     window.console.log(dimensions);
// };

export const Grid: Function = (): object => {
    return {
        foo: 1,
        bar: 2,
        baz: 'dus'
    };
};

// export { Grid };
